#include "main.h"

/**
 * _abs(int t) - Computes the absolute value of an integer
 * Description: The integer to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
