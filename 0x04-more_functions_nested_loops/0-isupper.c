#include "main.h"

/**
 * _isupper - check description
 * @x: The number to be checked
 * Description: The function checks if a letter is uppercase
 * Return: 1 for uppercase letter or 0 for anything else
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
